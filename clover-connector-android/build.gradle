group = 'com.clover.sdk'
version = '1.1.0.1'

apply plugin: 'android-library'

repositories {
  mavenLocal()
  maven { url "http://artifactory.corp.clover.com:8081/artifactory/repo" }
  mavenCentral()
  jcenter()
  flatDir {
    dirs '../lib'
  }
}

android {
  lintOptions {
    abortOnError false
  }
  compileSdkVersion 21
  buildToolsVersion "25.0.0"

  defaultConfig {
    minSdkVersion 16
    targetSdkVersion 16
  }

  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
defaultTasks 'install'

dependencies {
//  compile(name: 'clover-remote-interface-175', ext: 'aar')
//  compile(name: 'clover-android-sdk-148', ext: 'aar')
  compile 'com.clover:clover-remote-interface:180.4'
  compile 'com.clover.sdk:clover-android-sdk:160.1'
  compile 'com.google.code.gson:gson:2.3.1'
  compile 'com.neovisionaries:nv-websocket-client:1.31'
  testCompile 'junit:junit:4.12'
}
